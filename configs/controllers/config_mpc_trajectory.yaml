# `mpc:` block AUTO-GENERATED by launchers/simulation/scripts/sync_mpc_configs.py
# Source of truth: launchers/simulation/config/config.yaml (controller_manager.mpc block).
# Do NOT edit the mpc: block by hand — edit the source and rerun the sync.
# Other top-level keys (e.g. `controller:` with the OCP json paths) are
# preserved on each sync.
controller: {solver_definition_path: libs/acados_trajectory_mpc/configs/solver_definition.yaml,
  ocp_json_file_path: libs/acados_trajectory_mpc/mpc_generated_code/acados_ocp.json}
mpc:
  max_vel_percentage: 1.0
  parameters:
    mass: [1.14]
    Q: [10.0, 10.0, 20.0, 0.1, 0.1, 20.0, 0.1, 0.1, 0.1]
    Qe: [10.0, 10.0, 20.0, 0.1, 0.1, 30.0, 0.1, 0.1, 0.1]
    R: [0.8, 1.0, 1.0, 1.0]
  constraints:
    lbu: [0.01, -2.0, -2.0, -5.0]
    ubu: [40.0, 2.0, 2.0, 5.0]
