[pytest]
# Exclude deprecated tests that have unmet dependencies (e.g., rerun module).
# These tests are kept for historical reference but not maintained.
norecursedirs = deprecated
testpaths = pybind/tests viewer/tests
