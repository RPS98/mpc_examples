# All MAV showcase libs that used to live as submodules here have been
# elevated to <workspace>/mav_libs/ and are now managed by pmpc.repos
# (see the repo-root README). This file is kept empty to make the migration
# explicit and to avoid `git submodule init` confusion.
