mpc:
  N_horizon: 30 # Prediction steps
  tf: 2.0 # Prediction horizon in seconds
  constraints:
    # Indices of bounds on u at intermediate shooting nodes (0 to N-1)
    idxbu: [0, 1, 2, 3] # Bounded control inputs: [thrust, wx, wy, wz]
    # Lower bounds on nonlinear constraints at shooting nodes (1 to N)
    lh: [0.0] # h(x,u)=||v||^2, lower bound
    # Upper bounds on nonlinear constraints at shooting nodes (1 to N)
    uh: [0.0] # h(x,u)=||v||^2, upper bound (max speed squared)
    # Indices of soft nonlinear constraints at shooting nodes (1 to N)
    idxsh: [0] # Soft constraint on ||v||^2: allow bounded violation with penalty
solver:
  export_dir: 'libs/acados_position_mpc/'
  model_name: 'mpc_position'
  generate_c_code: true
  verbose: true
  cost_type: EXTERNAL
  qp_solver: PARTIAL_CONDENSING_HPIPM
  nlp_solver_type: SQP_RTI
  hessian_approx: EXACT
  integrator_type: ERK
integrator:
  generate_c_code: true
  integrator_type: ERK
  num_stages: 4
  num_steps: 3
cpp_module:
  generate: false  # C++ wrapper is committed under libs/acados_position_mpc/;
                   # only mpc_generated_code/ is regenerated by ./libs/generate_acados.sh.
  project_name: acados_position_mpc
